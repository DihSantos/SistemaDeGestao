@model List<FabricantesModel>
@{
    ViewData["Title"] = "Lista de Fabricantes";
}

<div class="d-grid d-md-flex">
    <a class="btn btn-primary" role="button" asp-controller="Home" asp-action="Index">Voltar</a>
</div>

<div class="text-center" style="margin-bottom:60px">

    @if (TempData["MensagemSucesso"] != null)
    {
        <div class="alert alert-success" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert">x</button>
            @TempData["MensagemSucesso"]
        </div>
    }

    @if (TempData["MensagemErro"] != null)
    {
        <div class="alert alert-danger" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert">x</button>
            @TempData["MensagemErro"]
        </div>
    }
    <br />

    <h3 style="color:#585858">Listagem de Fabricantes</h3>

    <br />

    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a class="btn btn-primary" role="button" asp-controller="Fabricantes" asp-action="Cadastrar">Adcionar novo fabricante</a>
    </div>

    <br />
    <br />

    <table class="display" id="table-fabricante" style="width:100%">
        <thead>
            <tr style="color:#585858">
                <th scope="col">#</th>
                <th scope="col">Fabricante</th>
                <th scope="col">País de Origem</th>
                <th scope="col">Ano de Fundação</th>
                <th scope="col">Website</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var fabricantes in Model)
                {
                    <tr>
                        <td scope="row">@fabricantes.Id</td>
                        <td>@fabricantes.Fabricante</td>
                        <td>@fabricantes.PaisOrigem</td>
                        <td>@fabricantes.AnoFundacao</td>
                        <td>@fabricantes.Website</td>
                        <td>
                            <div class="btn-group" role="group" aria-label="Basic example">
                                <a role="button" class="btn btn-primary gap-2" asp-route-id="@fabricantes.Id" asp-controller="Fabricantes" asp-action="Editar">Editar</a>
                                <a role="button" class="btn btn-danger gap-2" asp-route-id="@fabricantes.Id" asp-controller="Fabricantes" asp-action="DeletarConfirmacao">Deletar</a>
                            </div>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>

</div>

